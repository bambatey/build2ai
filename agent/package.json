{
  "name": "structai-agent",
  "version": "0.1.0",
  "description": "Local file-system bridge for StructAI. Exposes a watched folder over HTTP + SSE.",
  "private": true,
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "chokidar": "^4.0.3"
  },
  "engines": {
    "node": ">=20"
  }
}
